# 角色创建引导器

一个无需安装、无需服务器的半自动车卡工具。玩家可以像完成电子游戏的新手引导一样，逐步完成角色概念、27 点属性购点、背景、初始学派、OP 节点、熟练、装备、专长和最终角色卡。

## 直接使用

双击 `index.html` 即可使用。角色进度自动保存在当前浏览器中。

建议通过本地网页服务或 GitHub Pages 打开，以获得最稳定的浏览器体验；软件本身不依赖后端、数据库或账号。

## 部署到 GitHub Pages

1. 新建一个 GitHub 仓库。
2. 将本文件夹中的 `index.html`、`styles.css`、`data.js`、`app.js` 与 `.nojekyll` 上传到仓库根目录。
3. 打开仓库的 **Settings → Pages**。
4. 在 **Build and deployment** 中选择 **Deploy from a branch**。
5. 选择 `main` 分支和根目录 `/`，保存。

稍等片刻后，GitHub 会给出可公开访问的网址。所有角色资料只保存在玩家自己的浏览器中，不会上传到网站。

## 已实现的规则辅助

- 27 点购点与背景属性上限检查
- 18 项正式背景与自创背景预算向导
- 初始学派训练、技能重复规避与背景豁免自动选择
- 1–30 OP 预算与学派节点前置检查
- 多学派 OP 分配、初始学派／主学派区分与兼修顺序记录
- 6 组重度冲突、8 组轻度冲突与主学派一半上限自动检查
- 熟练加值、生命值、AC、先攻、技能、学派攻击与豁免 DC 自动计算
- 熟练武器筛选、武器伤害与标签速览
- OP 4／8／12／16 的专长或属性提升记录
- 角色形象／Token 上传、方形裁切压缩，并随存档和独立角色卡导出
- 设备本地自动保存、JSON 存档、独立 HTML 角色卡、打印与 PDF 导出

## 文件说明

- `index.html`：网页入口
- `styles.css`：界面与打印样式
- `data.js`：背景、学派、节点、武器与专长数据
- `app.js`：引导流程、校验、计算、保存与导出逻辑

这是纯静态网页；修改文件后直接刷新即可，不需要构建步骤。
